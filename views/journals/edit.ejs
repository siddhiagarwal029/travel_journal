<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600&family=Quicksand:wght@500;700&display=swap" rel="stylesheet">

    <title>Edit Journal</title>
</head>
<body>
    <%- include('../partials/header') %>
    <div class="container mt-5">
        <h2>Edit Journal Entry</h2>
        
        <form action="/journals/<%= journal._id %>?_method=PUT" method="POST" novalidate>
            <div class="form-group">
                <label for="destination">Destination</label>
                <input type="text" class="form-control" id="destination" name="destination" value="<%= journal.destination %>" readonly>
            </div>
            <div class="form-group">
                <label for="arrivalDate">Arrival Date</label>
                <input type="date" class="form-control" id="arrivalDate" name="arrivalDate" value="<%= journal.arrivalDate.toISOString().split('T')[0] %>" required>
                <div class="invalid-feedback">
                    Please provide a valid arrival date.
                </div>
            </div>
            <div class="form-group">
                <label for="departureDate">Departure Date</label>
                <input type="date" class="form-control" id="departureDate" name="departureDate" value="<%= journal.departureDate.toISOString().split('T')[0] %>" required>
                <div class="invalid-feedback">
                    Please provide a valid departure date.
                </div>
            </div>
            <div class="form-group">
                <label for="experience">Experience</label>
                <textarea class="form-control" id="experience" name="experience" rows="3" required><%= journal.experience %></textarea>
                <div class="invalid-feedback">
                    Please provide your experience.
                </div>
            </div>
            <div class="form-group">
                <label for="rating">Rating</label>
                <select class="form-control" id="rating" name="rating" required>
                    <option value="" disabled>Select rating</option>
                    <option value="1" <%= journal.rating == 1 ? 'selected' : '' %>>1</option>
                    <option value="2" <%= journal.rating == 2 ? 'selected' : '' %>>2</option>
                    <option value="3" <%= journal.rating == 3 ? 'selected' : '' %>>3</option>
                    <option value="4" <%= journal.rating == 4 ? 'selected' : '' %>>4</option>
                    <option value="5" <%= journal.rating == 5 ? 'selected' : '' %>>5</option>
                </select>
                <div class="invalid-feedback">
                    Please select a rating.
                </div>
            </div>
            <button type="submit" class="btn btn-primary">Update Journal</button>
        </form>
    </div>
    <%- include('../partials/footer') %>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/validation.js"></script>
</body>
</html>